name: empresa_botellones_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2 
  geolocator: ^10.0.0
  google_maps_flutter: ^2.2.0
  url_launcher: ^6.0.20

flutter:
  uses-material-design: true

  assets:
    - assets/logo.jpg  # Asegúrate de que esta ruta sea correcta

flutter_icons:
  android: true
  ios: true
  image_path: "assets/logo.jpg"  # Ruta a tu nuevo logo/icono
